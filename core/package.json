{
  "name": "agentworld-core",
  "private": true,
  "version": "0.1.0",
  "description": "Agent World — the standard and SDK (spec + packages)",
  "license": "Apache-2.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "tsup": "^8.3.0",
    "vitest": "^2.1.0"
  }
}
